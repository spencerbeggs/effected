# @effected/lockfiles constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `BunExtension` | Class | Extension data specific to bun lockfiles, attached to `Lockfile.extension` when the format is `"bun"`. | |
| `ImporterDependency` | Class | One declared dependency of one workspace importer, as the lockfile records it. | |
| `Lockfile` | Class | The unified lockfile model all four formats normalize into. | |
| `LockfileFormat` | Variable + TypeAlias | The union of supported lockfile format names. | |
| `LockfileFramingError` | Class | Failure of `Lockfile.parse`: the content parsed as text, but no single lockfile document could be located in it. | |
| `LockfileImporter` | Class | One workspace importer's declared dependencies, as the lockfile records them. | |
| `LockfileIntegrity` | Class | Result of checking a parsed lockfile against the workspace's declared manifests. | |
| `LockfileParseError` | Class | Failure of `Lockfile.parse`: the given content is not a valid lockfile of the requested format. | |
| `PnpmCatalogs` | TypeAlias | The pnpm `catalogs:` record shape as it appears in `pnpm-lock.yaml`: catalog name → package name → pinned version string or `{ specifier, version }` pair. | |
| `PnpmExtension` | Class | Extension data specific to pnpm lockfiles, attached to `Lockfile.extension` when the format is `"pnpm"`. | |
| `ResolvedPackage` | Class | A package resolved from a lockfile. | |
| `UnsupportedLockfileVersion` | Interface | The cause a {@link LockfileParseError} carries when a lockfile predates the supported format version. | |
| `WorkspaceDependency` | Class | A directed dependency edge between two workspace packages as recorded in the lockfile. | |
| `WorkspaceManifest` | Class | The minimal manifest shape {@link LockfileIntegrity.compare} checks a lockfile against: a package name plus the four optional dependency maps. | |
| `filenameFor` | Function | The conventional lockfile filename for a format: `"bun.lock"`, `"package-lock.json"`, `"pnpm-lock.yaml"` or `"yarn.lock"`. | |
| `filenamesFor` | Function | Every filename a format is genuinely written under, primary first. | |
| `fromFilename` | Function | The format a lockfile filename identifies, if any. | |
| `isUnsupportedLockfileVersion` | Function | Whether a `LockfileParseError.cause` is an {@link UnsupportedLockfileVersion}. | |
