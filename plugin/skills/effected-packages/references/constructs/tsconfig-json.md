# @effected/tsconfig-json constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `CompilerOptions` | Variable + Namespace | Type-only companion namespace for {@link (CompilerOptions:variable)}, exposing its decoded and encoded shapes. | tsconfig compilerOptions schema, target module lib jsx strict flags, forward-tolerant passthrough |
| `EnumFamily` | TypeAlias | The nine `compilerOptions` / `watchOptions` enum families this codec knows, per R1.6. | |
| `FallbackPolling` | Variable | `watchOptions.fallbackPolling`. | watchOptions.fallbackPolling enum, polling strategy when native watching is unavailable |
| `FindNearestOptions` | Interface | Options for {@link TsconfigDiscovery.findNearest}. | |
| `Jsx` | Variable | `compilerOptions.jsx`. There is no `none` literal — tsc's option map has only these five. | compilerOptions.jsx enum, react-jsx react-jsxdev preserve classic transform |
| `JsxConfig` | Class | The JSX transform configuration a `jsx` compiler option implies: which runtime (`"automatic"` for `react-jsx` / `react-jsxdev`, `"classic"` for `react`) and, for the automatic runtime, the import source the transform emits (`jsxImportSource`, defaulting to `"react"` per tsc). | derive bundler jsx runtime from compilerOptions, automatic vs classic transform, jsxImportSource default |
| `Lib` | Variable | `compilerOptions.lib` member values — the complete TS 6.0.3 set, lowercase canonical, per R1.2. | compilerOptions.lib enum, dom es2015 esnext lib file names |
| `Module` | Variable | `compilerOptions.module` — the module output format. `none`, `amd`, `umd` and `system` are deprecated in TS 6.0. | compilerOptions.module enum, commonjs esnext nodenext output format |
| `ModuleDetection` | Variable | `compilerOptions.moduleDetection`. | compilerOptions.moduleDetection enum, auto legacy force |
| `ModuleResolution` | Variable | `compilerOptions.moduleResolution`. `node10`, `node` and `classic` are deprecated in TS 6.0. | compilerOptions.moduleResolution enum, node10 bundler nodenext |
| `NewLine` | Variable | `compilerOptions.newLine`. | compilerOptions.newLine enum, crlf lf line ending |
| `PortableTsconfig` | Class + Interface | The portable-tsconfig filter: {@link (PortableTsconfig:class).make} narrows a resolved or bare compiler-options object to the allow-listed, machine-independent subset described on {@link (PortableTsconfig:interface)}. | project compilerOptions for a virtual typescript environment, twoslash api-extractor sandbox, strip absolute paths and emit options, allow-list filter |
| `PortableTsconfigOptions` | Interface | Options for {@link (PortableTsconfig:class).make}. | |
| `ProgrammaticCompilerOptions` | Interface | The shape {@link TsEnumCodec.encodeCompilerOptions} returns: the numeric-enum-encoded `compilerOptions` a virtual-TS environment and the TypeScript compiler API consume programmatically. | |
| `ProgrammaticCompilerOptionsValue` | TypeAlias | A single value a programmatic `compilerOptions` entry can hold — a structural transcription of TypeScript's own `CompilerOptionsValue`, transcribed (not imported) to honor this package's zero-`typescript` rule. | |
| `Reference` | Variable + Namespace | Type-only companion namespace for {@link (Reference:variable)}. | tsconfig references entry, project reference path |
| `ResolvedTsconfig` | Class + Interface | The pure extends-merge engine: parse-time path absolutization ({@link (ResolvedTsconfig:class).absolutize}), the per-field merge fold ({@link (ResolvedTsconfig:class).merge}), and the `${configDir}` final phase ({@link (ResolvedTsconfig:class).substituteConfigDir}). | merge a tsconfig extends chain, absolutize compiler option paths, configDir template substitution, pure extends-merge engine |
| `SyncFileSystem` | Interface | The synchronous file operations {@link TsconfigLoaderSync} needs, supplied by the consumer. Node's built-ins satisfy it directly: ```ts import { existsSync, readFileSync } from "node:fs"; | |
| `SyncPath` | Interface | The synchronous path operations {@link TsconfigLoaderSync} needs, supplied by the consumer. Deliberately a structural subset of `node:path`, so the built-in module (and its `win32` / `posix` variants, or a Bun / Deno equivalent) satisfies it verbatim: ```ts import * as path from "node:path"; | |
| `Target` | Variable | `compilerOptions.target` — the ECMAScript target. `es5` is deprecated in TS 6.0; `es3` has no literal (dead per R1.3 — a `target: "es3"` value fails decode against this schema rather than silently passing through, since `target` itself is a live, typed field). | compilerOptions.target enum, ecmascript version es2015 esnext |
| `TsEnumCodec` | Class | The string↔numeric enum codec for `compilerOptions` / `watchOptions` families, per R1.6. Plain data: every lookup is a synchronous map read returning `Option.Option`, never a thrown error. | convert between tsc string enum spellings and numeric enum values, encode compilerOptions for the compiler api, normalize lib file name spelling |
| `TsconfigDiscovery` | Class | Nearest-config upward discovery for `tsconfig.json`. | find the governing tsconfig, nearest tsconfig.json upward search from a starting directory |
| `TsconfigExtendsError` | Class | Raised when a config's `extends` chain cannot be resolved: an unresolvable target (`"not-found"`), a re-entrant chain (`"cycle"`), a chain deeper than `MAX_EXTENDS_DEPTH` (`"depth"`), or an empty target string (`"empty"`). `path` is the config whose `extends` failed, `target` is what it tried to extend (the offending spec for `"not-found"`/`"empty"`, the re-entered config path for `"cycle"`, the refused config for `"depth"`), and `chain` is the full resolution chain of normalized absolute paths. | handle a broken tsconfig extends chain, unresolvable target, cycle, depth limit, empty extends spec |
| `TsconfigJson` | Variable + Namespace | Type-only companion namespace for {@link (TsconfigJson:variable)}, exposing its decoded and encoded shapes plus the JSONC codec. | tsconfig.json document schema, compilerOptions files include exclude references watchOptions typeAcquisition |
| `TsconfigJsonFromString` | Variable | Decodes a JSONC-encoded tsconfig.json document straight into {@link (TsconfigJson:variable)}. Bound once at module top level — `Jsonc.schema` is schema-producing, and this is the shared instance (the house `FromString` idiom, R3.3; `TsconfigJson` is a `Schema.StructWithRest` value rather than a `Schema.Class`, so the codec is a sibling export, not a static). | parse tsconfig.json text with comments, jsonc codec for tsconfig documents |
| `TsconfigLoader` | Class | The tsconfig.json loader: {@link TsconfigLoader.load} reads and decodes one config file, {@link TsconfigLoader.resolve} runs the full load -\> extends -\> merge -\> `${configDir}` pipeline, and {@link TsconfigLoader.compilerOptions} projects the resolved result down to its merged `compilerOptions`. | resolve tsconfig extends chain, read decode and merge a tsconfig.json and its bases, effectful io pipeline |
| `TsconfigLoaderSync` | Class | The synchronous tsconfig.json loader facade: the {@link TsconfigLoader} pipeline run under `Effect.runSyncExit` against consumer-supplied {@link SyncFileSystem} and {@link SyncPath} operations. For sync-only host APIs (bundler plugin hooks, config factories); everything else should use {@link TsconfigLoader} with real platform layers. | resolve tsconfig extends chain synchronously, bundler plugin hook config factory, consumer-supplied file and path operations |
| `TsconfigLoaderSyncOptions` | Interface | The consumer-supplied operations backing one {@link TsconfigLoaderSync} call: the file operations and the path implementation. Both are required — this package never imports `node:*` and never assumes posix, so the platform binding is entirely the caller's. | |
| `TsconfigParseError` | Class | Raised when a tsconfig.json document fails to parse or decode. `path` is the file path when the failure is file-bound, and the empty string otherwise (e.g. decoding an in-memory string). The loader (Task 8) wraps file-bound decode failures in this error; this module only declares it. | handle a tsconfig.json that fails to parse or decode |
| `TypeAcquisition` | Variable + Namespace | Type-only companion namespace for {@link (TypeAcquisition:variable)}. | tsconfig typeAcquisition schema, enable include exclude automatic type acquisition |
| `WatchDirectory` | Variable | `watchOptions.watchDirectory`. | watchOptions.watchDirectory enum, directory watching strategy |
| `WatchFile` | Variable | `watchOptions.watchFile`. | watchOptions.watchFile enum, file watching strategy, polling vs fsevents |
| `WatchOptions` | Variable + Namespace | Type-only companion namespace for {@link (WatchOptions:variable)}. | tsconfig watchOptions schema, watchFile watchDirectory fallbackPolling exclude |
