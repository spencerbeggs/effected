# @effected/schema-org constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `APIReference` | Class | A schema.org `APIReference` — documentation of an API surface. | |
| `ConflictingTermError` | Class | Indicates that a key in a node's `additional` catch-all collides with a typed field on that node, or with `@id` or `@type`. | |
| `Conformance` | Class | The offline conformance gate: does schema.org define this `@type`, and is every property on it legal for that type? | from `@effected/schema-org/validate` |
| `ConformanceIssue` | Variable + TypeAlias | Anything `Conformance.check` can report. | from `@effected/schema-org/validate` |
| `ConformanceOptions` | Interface | Which issue kinds fail `Conformance.validateResult`. | from `@effected/schema-org/validate` |
| `CreativeWork` | Class | A schema.org `CreativeWork` — the general node for a created work, and the base vocabulary the more specific nodes in this package extend. | |
| `CreativeWorkFields` | Variable | The fields shared by every `CreativeWork` descendant this package models, spread into `CreativeWork`, `SoftwareSourceCode`, `TechArticle` and `APIReference`. | |
| `DanglingReference` | Class | A reference to an `@id` no node in the graph defines. | from `@effected/schema-org/validate` |
| `DeprecatedProperty` | Class | A property schema.org has deprecated. Valid but flagged, exactly as `DeprecatedType` is. | from `@effected/schema-org/validate` |
| `DeprecatedType` | Class | A node whose `@type` schema.org has deprecated. | from `@effected/schema-org/validate` |
| `DuplicateNodeIdError` | Class | Indicates that two nodes in one graph claim the same `@id`. | |
| `HasNodeId` | Interface | Anything carrying an `@id`. Every node class in this package satisfies it. | |
| `InvalidNodeIdError` | Class | Indicates that a string is not usable as a JSON-LD node identifier: it is empty, contains whitespace, or contains a control character. | |
| `JsonLdDocument` | Class | A JSON-LD document: `@context` plus a flat `@graph` of nodes that reference each other by `@id`. | |
| `JsonLdNode` | Variable + TypeAlias | Any node this package can place in a graph. | |
| `NodeId` | Variable | A JSON-LD node identifier: a non-empty string carrying no whitespace and no control characters. | |
| `NodeRef` | Class | A reference from one node to another: the `{"@id": "…"}` form. | |
| `NonConformantGraphError` | Class | Indicates that a graph carries at least one conformance issue of a kind the gate was configured to fail on. | from `@effected/schema-org/validate` |
| `Organization` | Class | A schema.org `Organization` — a company, project or team that authors or publishes a work. | |
| `Person` | Class | A schema.org `Person` — an author, maintainer or contributor. | |
| `PropertyNotOnType` | Class | A property schema.org defines, used on a type it is not legal on. | from `@effected/schema-org/validate` |
| `SoftwareSourceCode` | Class | A schema.org `SoftwareSourceCode` — the node describing a package's source. | |
| `TechArticle` | Class | A schema.org `TechArticle` — a piece of technical documentation. | |
| `TechArticleFields` | Variable | The fields shared by `TechArticle` and its descendant `APIReference`. | |
| `TermKind` | Variable + TypeAlias | Which kind of term a `UnknownTerm` issue is about. | from `@effected/schema-org/validate` |
| `ThingFields` | Variable | The fields every schema.org node in this package carries, spread into each node class rather than inherited. | |
| `UnknownTerm` | Class | A term the vendored schema.org vocabulary does not define at all. | from `@effected/schema-org/validate` |
| `Vocabulary` | Class | A read API over the vendored schema.org vocabulary: which terms exist, how the class hierarchy runs, and which properties a type may legally carry. | from `@effected/schema-org/validate` |
