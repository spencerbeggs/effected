# @effected/schema-org constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `APIReference` | Class | A schema.org `APIReference` — documentation of an API surface. | schema.org APIReference node, structured data for a documented api surface |
| `ConflictingTermError` | Class | Indicates that a key in a node's `additional` catch-all collides with a typed field on that node, or with `@id` or `@type`. | handle a catch-all key colliding with a typed field, @id or @type when building a json-ld document |
| `Conformance` | Class | The offline conformance gate: does schema.org define this `@type`, and is every property on it legal for that type? | from `@effected/schema-org/validate` — validate a json-ld document against schema.org offline, gate structured data in ci without a live validator |
| `ConformanceIssue` | Variable + TypeAlias | Anything `Conformance.check` can report. | from `@effected/schema-org/validate` — the union of everything conformance checking can report about a json-ld document |
| `ConformanceOptions` | Interface | Which issue kinds fail `Conformance.validateResult`. | from `@effected/schema-org/validate` |
| `CreativeWork` | Class | A schema.org `CreativeWork` — the general node for a created work, and the base vocabulary the more specific nodes in this package extend. | schema.org CreativeWork node, the general created-work vocabulary other nodes extend |
| `CreativeWorkFields` | Variable | The fields shared by every `CreativeWork` descendant this package models, spread into `CreativeWork`, `SoftwareSourceCode`, `TechArticle` and `APIReference`. | the shared CreativeWork field set, spread into a node class you are composing |
| `DanglingReference` | Class | A reference to an `@id` no node in the graph defines. | from `@effected/schema-org/validate` — a json-ld reference pointing at an @id no node in the document defines, legal but reportable |
| `DeprecatedProperty` | Class | A property schema.org has deprecated. Valid but flagged, exactly as `DeprecatedType` is. | from `@effected/schema-org/validate` — a schema.org property that is deprecated, valid but flagged |
| `DeprecatedType` | Class | A node whose `@type` schema.org has deprecated. | from `@effected/schema-org/validate` — a schema.org type that is deprecated, valid but flagged |
| `DuplicateNodeIdError` | Class | Indicates that two nodes in one graph claim the same `@id`. | handle two json-ld nodes claiming the same @id when assembling a document |
| `HasNodeId` | Interface | Anything carrying an `@id`. Every node class in this package satisfies it. | |
| `InvalidNodeIdError` | Class | Indicates that a string is not usable as a JSON-LD node identifier: it is empty, contains whitespace, or contains a control character. | handle a json-ld @id that is empty, has whitespace or carries a control character |
| `JsonLdDocument` | Class | A JSON-LD document: `@context` plus a flat `@graph` of nodes that reference each other by `@id`. | assemble schema.org nodes into one json-ld document and serialize it safely into a script element |
| `JsonLdNode` | Variable + TypeAlias | Any node this package can place in a graph. | the union of node types a json-ld document can hold |
| `NodeId` | Variable | A JSON-LD node identifier: a non-empty string carrying no whitespace and no control characters. | the json-ld node identifier rule as a schema, reuse the @id validity check by identity |
| `NodeRef` | Class | A reference from one node to another: the `{"@id": "…"}` form. | point one schema.org node at another by @id instead of embedding it |
| `NonConformantGraphError` | Class | Indicates that a graph carries at least one conformance issue of a kind the gate was configured to fail on. | from `@effected/schema-org/validate` — handle a json-ld document failing its schema.org conformance gate |
| `Organization` | Class | A schema.org `Organization` — a company, project or team that authors or publishes a work. | schema.org Organization node, structured data for a company or group |
| `Person` | Class | A schema.org `Person` — an author, maintainer or contributor. | schema.org Person node, structured data for an author or maintainer |
| `PropertyNotOnType` | Class | A property schema.org defines, used on a type it is not legal on. | from `@effected/schema-org/validate` — a property schema.org does not define on the type carrying it, including by inheritance |
| `SoftwareSourceCode` | Class | A schema.org `SoftwareSourceCode` — the node describing a package's source. | schema.org SoftwareSourceCode node, structured data for a package's source |
| `TechArticle` | Class | A schema.org `TechArticle` — a piece of technical documentation. | schema.org TechArticle node, structured data for a documentation page |
| `TechArticleFields` | Variable | The fields shared by `TechArticle` and its descendant `APIReference`. | the shared TechArticle field set, spread into a node class you are composing |
| `TermKind` | Variable + TypeAlias | Which kind of term a `UnknownTerm` issue is about. | from `@effected/schema-org/validate` — whether a conformance issue is about a schema.org type or a property |
| `ThingFields` | Variable | The fields every schema.org node in this package carries, spread into each node class rather than inherited. | the shared Thing field set every schema.org node in this package carries |
| `UnknownTerm` | Class | A term the vendored schema.org vocabulary does not define at all. | from `@effected/schema-org/validate` — a term schema.org does not define at all, reported and never silently passed |
| `Vocabulary` | Class | A read API over the vendored schema.org vocabulary: which terms exist, how the class hierarchy runs, and which properties a type may legally carry. | from `@effected/schema-org/validate` — ask the vendored schema.org vocabulary directly about types, properties and inheritance |
