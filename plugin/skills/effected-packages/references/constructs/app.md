# @effected/app constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `App` | Class | The application control plane: one layer wiring `Xdg`, `AppDirs`, `Store` and `Cache` to the same namespace. | |
| `AppCache` | Class | The cache-directory database glue: a TTL `Cache` whose file lives in the ambient `AppDirs` cache directory. | |
| `AppCacheOptions` | Interface | Options for {@link AppCache.layer}. | |
| `AppConfig` | Class | The xdg-flavored `ConfigFile` preset: discovery through the app's XDG config search path, saves into the app's own config directory. | |
| `AppConfigOptions` | Interface | Options for {@link AppConfig.layer}. | |
| `AppError` | TypeAlias | Everything that can come out of the control plane, for the application edge's `catchTags` block. | |
| `AppOptions` | Interface | Options for {@link App.layer}. | |
| `AppStore` | Class | The state-directory database glue: a migrated SQLite `Store` whose file lives in the ambient `AppDirs` state directory. | |
| `AppStoreOptions` | Interface | Options for {@link AppStore.layer}. | |
| `AppTestOptions` | Interface | Options for {@link App.layerTest}. | |
