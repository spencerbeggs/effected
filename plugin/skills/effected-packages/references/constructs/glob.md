# @effected/glob constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `GlobPattern` | Class | A compiled glob pattern: the schema IS the domain class. One encoded field, `source`; the compiled matcher lives in a private field the schema never encodes, built lazily for `make`/decode-constructed instances and pre-warmed by {@link GlobPattern.compile}. | compile a glob pattern, test if a path matches, minimatch dialect |
| `GlobPatternError` | Class | Typed failure raised when a glob pattern trips a compile-time guard: over-length, brace-expansion budget exhaustion, or nesting past the depth cap. Malformed input is never a defect — this is the only failure the package's fallible boundaries ({@link GlobPattern.compile} and `GlobSet.compile`) can produce. | handle malformed glob pattern, pattern too long, brace expansion budget exceeded, nesting depth exceeded |
| `GlobPatternOptions` | Class | The full minimatch options surface, schema-validated. Invalid options are a developer wiring error and throw at `make` — a defect at construction; the typed channel stays reserved for malformed patterns. | configure glob matching: dialect, dot files, case sensitivity, brace expansion, extglob |
| `GlobSet` | Class | A compiled multi-pattern include/exclude set: `matches(candidate)` is true when some include accepts it and no exclude does. One encoded field, `patterns` — the source text of every member, preserved verbatim; the classified indexes live in a private field the schema never encodes. | match a path against multiple include and exclude glob patterns, negated exclusion |
