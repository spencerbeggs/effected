# @effected/jsonc constructs

<!-- GENERATED by plugin/scripts/generate-constructs.mts — do not edit.
     Edit plugin/scripts/construct-annotations.json and regenerate. -->

| Construct | Kind | Purpose | Reach for it when |
| --- | --- | --- | --- |
| `Jsonc` | Class | Static entry points for JSONC parsing, editing-adjacent utilities and the schema factories. Not instantiable. | |
| `JsoncBoundCodec` | Interface | A domain codec pre-bound to its two directions, returned by {@link Jsonc.bind}: the composed `schema` (what {@link Jsonc.schema} returns) plus `decode` and `encode` functions derived from it once, so callers need no generic `Schema` machinery at the use site. | |
| `JsoncCanonicalizeError` | Class | Canonicalization failure: a `JsoncCanonicalizeErrorCode` naming the failure mode, the JSON-pointer `path` to the offending value (`""` is the document root) and a human-readable `detail`. Raised by {@link JsoncFingerprint.canonicalize}, {@link JsoncFingerprint.canonicalizeResult} and {@link JsoncFingerprint.hash}. | |
| `JsoncCanonicalizeErrorCode` | Variable + TypeAlias | The union of all canonicalize-error code string literals. | |
| `JsoncCanonicalizeError_base` | Variable | | |
| `JsoncEdit` | Class | A non-mutating text edit: replace the span `[offset, offset + length)` with `content`. Set `length` to `0` to insert, `content` to `""` to delete. | |
| `JsoncEdit_base` | Variable | | |
| `JsoncFingerprint` | Class | Static entry points for canonical JSON serialization (RFC 8785, the JSON Canonicalization Scheme) and SHA-256 content fingerprints. Not instantiable. | |
| `JsoncFormatter` | Class | Pure JSONC formatting statics. Not instantiable. | |
| `JsoncFormattingOptions` | Class | Options controlling JSONC formatting. All fields are omissible. | |
| `JsoncFormattingOptionsLike` | TypeAlias | Formatting options accepted at call sites: either a {@link JsoncFormattingOptions} instance or a plain literal with the same fields (the two are structurally interchangeable — only the option fields are read). Mirrors the `YamlRangeLike` posture in `@effected/yaml`, so a caller can pass `{ insertSpaces: false, tabSize: 2 }` without constructing the class. `JsoncFormattingOptions` remains the canonical decoded form. | |
| `JsoncFormattingOptions_base` | Variable | | |
| `JsoncModificationError` | Class | Raised when `JsoncModifier.modify` cannot navigate the requested path: the value at `depth` is not the container kind (`expected`) the next path segment requires. | |
| `JsoncModificationError_base` | Variable | | |
| `JsoncModifier` | Class | Structural JSONC modification statics. Not instantiable. | |
| `JsoncModifyOptions` | Interface | Options for `JsoncModifier.modify`: formatting controls for generated text. | |
| `JsoncNode` | Class | An immutable JSONC AST node produced by `Jsonc.parseTree`. | |
| `JsoncNodeType` | Variable + TypeAlias | The union of all JSONC AST node type string literals. | |
| `JsoncNode_base` | Variable | | |
| `JsoncParseError` | Class | Error-recovery parse failure: aggregates every {@link JsoncParseErrorDetail} encountered, so a single failure reports the whole batch. Raised by {@link Jsonc.parse}, {@link Jsonc.parseTree} and the decode direction of the schema factories. | |
| `JsoncParseErrorCode` | Variable + TypeAlias | The union of all JSONC parse-error code string literals. | |
| `JsoncParseErrorDetail` | Class | One recovered parse error: its `JsoncParseErrorCode` and its exact position (`offset`/`length`, plus zero-based `line`/`character`). A single {@link JsoncParseError} reports a batch of these. | |
| `JsoncParseErrorDetail_base` | Variable | | |
| `JsoncParseError_base` | Variable | | |
| `JsoncParseOptions` | Class | Options controlling parse behavior. All fields are omissible. | |
| `JsoncParseOptions_base` | Variable | | |
| `JsoncPath` | TypeAlias | An ordered sequence of {@link JsoncSegment} values describing a location within a JSONC document tree. | |
| `JsoncRange` | Class | A range within a JSONC document, expressed as a zero-based character `offset` and a `length` in UTF-16 code units. Pass to `JsoncFormatter.format` to restrict formatting to a region. | |
| `JsoncRange_base` | Variable | | |
| `JsoncSegment` | TypeAlias | A single path segment: a `string` for object property keys or a `number` for array indices. | |
| `JsoncStringifyError` | Class | Stringification failure: a `JsoncStringifyErrorCode` naming the failure mode, a human-readable `detail` (the engine's message for thrown cases — on V8 the circular-reference message includes the offending property path), and the offending `value`. Raised by {@link Jsonc.stringify}, {@link Jsonc.stringifyResult} and the encode direction of the schema factories. | |
| `JsoncStringifyErrorCode` | Variable + TypeAlias | The union of all JSONC stringify-error code string literals. | |
| `JsoncStringifyError_base` | Variable | | |
| `JsoncStringifyOptions` | Class | Options controlling stringify behavior. All fields are omissible; the vocabulary matches `JsoncFormattingOptions`. | |
| `JsoncStringifyOptions_base` | Variable | | |
| `JsoncTextHashOptions` | Class | Options controlling {@link JsoncFingerprint.hashText}. All fields are omissible. | |
| `JsoncTextHashOptions_base` | Variable | | |
| `JsoncVisitor` | Class | SAX-style JSONC visitor statics. Not instantiable. | |
| `JsoncVisitorEvent` | Variable + TypeAlias | The discriminated union of JSONC visitor events. Every variant carries `offset` and `length`; `ObjectBegin`, `ArrayBegin`, `ObjectProperty` and `LiteralValue` also carry `path` context (the location being entered). | |
