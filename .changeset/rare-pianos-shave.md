---
"@effected/package-json": patch
---

## Documentation

- Corrected the README's list of `workspace:` range modifiers, where a find-and-replace had turned `workspace:~` into a second `workspace:^`. The three specifiers are `workspace:*`, `workspace:^` and `workspace:~`.
