---
"@effected/claude-code-plugin": patch
---

## Other

- Skill references now document `check`'s orphaned-catalog failure (reported `orphaned`, remedy is deleting the file or restoring a `catalog` block — `build` never deletes it), matching the CLI's behavior since #746.
