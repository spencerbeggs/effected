---
"@effected/claude-code-plugin": patch
"@effected/copilot-plugin": patch
---

## Other

- The generated construct index under `skills/effected-packages/references/constructs/jsonc.md` picks up `@effected/jsonc`'s synchronous fingerprint twins: the new `JsoncDigest` type, and `JsoncCanonicalizeError`'s updated summary naming `hashResult` and `hashTextResult` among its raisers. Regenerated output only — no skill prose or annotation changed.
