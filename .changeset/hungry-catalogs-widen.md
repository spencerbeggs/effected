---
"@effected/pnpm-plugin-effect": minor
---

## Features

- The managed `catalog:effected` now covers `@effected/engine` (`^0.1.0`, peer `^0.1.0`, `lock-minor`), the kit's new platform-free primitives package.
