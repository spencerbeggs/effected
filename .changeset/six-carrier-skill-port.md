---
"@effected/copilot-plugin": minor
---

## Features

- Ports the same carrier-pattern and `effect-v4-mcp` skill updates as
  `@effected/claude-code-plugin`: both bin postures (carrier-only,
  recommended, and shared, with its provenance cost), the `npx -p`
  major-pinned loader fallback routed through the carrier, and the
  `McpToolkit.unionTool` / `unionHandler` plus `@effected/mcp/guard` teaching
  with the corrected whole-bundle `Layer.fresh` stdio isolation rule.
