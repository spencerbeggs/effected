{
	"repos": {
		"effect": {
			"url": "https://github.com/Effect-TS/effect.git",
			"ref": "effect@4.0.0-rc.109",
			"purpose": "Read-only Effect v4 source pinned to the tag matching the effect catalog — the authority on what v4 actually exports, plus the v3→v4 migration notes and the @effect/vitest reference implementation. Tracks the main Effect-TS/effect monorepo (v4 development moved back there on 2026-07-19 when the effect-smol repo was archived); vendored at .repos/effect since the 2026-07-24 rename, matching the path a consuming repo would use.",
			"sparse": [
				"packages/effect",
				"packages/vitest",
				"migration",
				"ai-docs",
				"LLMS.md",
				"MIGRATION.md"
			],
			"orientation": {
				"layout": "Sparse checkout: packages/effect (core source + package.json exports map), packages/vitest (@effect/vitest reference implementation), migration/ (per-domain v3→v4 notes), ai-docs/, LLMS.md, MIGRATION.md.",
				"keyPaths": {
					"effect-source": "packages/effect/src",
					"unstable-modules": "packages/effect/src/unstable",
					"migration-notes": "migration",
					"vitest-reference": "packages/vitest/src"
				},
				"startHere": "packages/effect/src settles what v4 actually exports (evidence-ladder rung 2); migration/*.md settles v3→v4 renames (rung 1). Before trusting an answer, check the pin matches the installed effect version — node_modules wins on disagreement."
			}
		},
		"commonmark-js": {
			"url": "https://github.com/commonmark/commonmark.js.git",
			"ref": "0.31.2",
			"purpose": "Port base for @effected/markdown: the CommonMark reference parser (two-phase blocks/inlines algorithm, BSD-2) pinned at spec 0.31.2",
			"sparse": [
				"lib",
				"test",
				"LICENSE"
			],
			"orientation": {
				"layout": "Sparse checkout: lib/ (the parser: blocks.js block pass, inlines.js inline pass, node.js AST, render/ excluded from the port), test/ (spec.txt copy, regression.txt, smart_punct.txt).",
				"keyPaths": {
					"block-pass": "lib/blocks.js",
					"inline-pass": "lib/inlines.js",
					"ast-node": "lib/node.js",
					"regression-corpus": "test/regression.txt"
				},
				"startHere": "lib/blocks.js then lib/inlines.js — the two-phase algorithm the @effected/markdown engine ports. Port deltas are decided in the design doc (.claude/design/effected/packages/markdown.md): mdast node names, byte offsets, keep definitions/references unresolved. lib/render is out of scope (no product HTML)."
			}
		},
		"commonmark-spec": {
			"url": "https://github.com/commonmark/commonmark-spec.git",
			"ref": "0.31.2",
			"purpose": "CommonMark 0.31.2 spec text and conformance tooling (spec.txt, spec_tests.py, normalize.py) — source of the 652-example corpus for @effected/markdown",
			"sparse": [
				"spec.txt",
				"test",
				"LICENSE"
			],
			"orientation": {
				"layout": "Sparse checkout: spec.txt (the CommonMark 0.31.2 spec; 652 embedded examples), test/ (spec_tests.py extracts examples, normalize.py is the HTML-normalization reference for the test-only writer).",
				"keyPaths": {
					"spec": "spec.txt",
					"example-extractor": "test/spec_tests.py",
					"html-normalizer": "test/normalize.py"
				},
				"startHere": "spec.txt's appendix 'A parsing strategy' is the algorithm contract. Extract the conformance corpus with spec_tests.py --dump-tests (or vendor the published spec.json); normalize.py defines the HTML equivalence the __test__/ harness reimplements. Test data is CC-BY-SA 4.0 — vendor test-only with attribution."
			},
			"notes": [
				{
					"id": "n-34b9",
					"date": "2026-07-18",
					"ref": "0.31.2",
					"note": "Running test/spec_tests.py drops test/__pycache__/ into the checkout, which shows as dirty in repos status forever (the .repos tripwires block git clean inside the submodule, and sync does not fix dirt). Always invoke it as PYTHONDONTWRITEBYTECODE=1 python3 test/spec_tests.py ... — avoiding the write is the only clean path. The existing __pycache__ from the 2026-07-18 corpus generation is harmless untracked bytecode."
				}
			]
		},
		"cmark-gfm": {
			"url": "https://github.com/github/cmark-gfm.git",
			"ref": "0.29.0.gfm.13",
			"purpose": "GFM spec text and corpora (test/spec.txt extension sections, extensions.txt incl. footnotes, pathological tests) plus C reference implementations of the GFM constructs",
			"sparse": [
				"test",
				"extensions",
				"src",
				"COPYING"
			],
			"orientation": {
				"layout": "Sparse checkout: test/ (spec.txt with the GFM extension sections, extensions.txt incl. the only official footnote corpus, pathological_tests.py, fuzzing assets), extensions/ (C implementations: table, strikethrough, autolink, tasklist, tagfilter), src/ (cmark core incl. footnotes).",
				"keyPaths": {
					"gfm-spec": "test/spec.txt",
					"extension-corpus": "test/extensions.txt",
					"pathological-suite": "test/pathological_tests.py",
					"construct-impls": "extensions"
				},
				"startHere": "test/spec.txt extension sections (Tables/Task list items/Strikethrough/Autolinks/Disallowed Raw HTML, 24 examples) + test/extensions.txt (30 more, incl. footnotes) are the P2 corpora; test/pathological_tests.py is the linear-time guarantee suite every dialect must pass. extensions/*.c are semantic references only — the port base is commonmark.js, not this C."
			}
		},
		"mdast": {
			"url": "https://github.com/syntax-tree/mdast.git",
			"ref": "5.0.0",
			"purpose": "The mdast spec prose (readme.md IS the spec) — the node-type contract @effected/markdown's Schema classes mirror, incl. the GFM extension node set",
			"orientation": {
				"layout": "Full checkout; readme.md is the entire spec (unist-based node types, mixins, enumerations).",
				"keyPaths": {
					"spec": "readme.md"
				},
				"startHere": "readme.md — the 19 CommonMark node types plus the GFM section (delete, table/tableRow/tableCell, listItem.checked, footnoteDefinition/footnoteReference) are the names and field shapes MarkdownNode.ts must mirror exactly; the Mdast projection is spec-valid only if these match."
			}
		},
		"mdast-util-from-markdown": {
			"url": "https://github.com/syntax-tree/mdast-util-from-markdown.git",
			"ref": "2.0.3",
			"purpose": "Reference for event-to-mdast tree assembly and the 27 position-complete .md/.json AST fixtures used as @effected/markdown's interop corpus",
			"sparse": [
				"dev",
				"test",
				"license",
				"readme.md"
			],
			"orientation": {
				"layout": "Sparse checkout: dev/lib/index.js (the readable event-to-mdast assembler), test/index.js (the CommonMark-conformance-without-product-HTML harness precedent), test/fixtures/ (27 .md/.json pairs with full unist positions).",
				"keyPaths": {
					"tree-assembler": "dev/lib/index.js",
					"harness-precedent": "test/index.js",
					"ast-fixtures": "test/fixtures"
				},
				"startHere": "test/fixtures/*.json are the position-complete mdast trees the Mdast projection is asserted against (P5 corpus). test/index.js shows the normalized-HTML conformance technique for a library that ships no HTML renderer — the pattern __test__/e2e reimplements."
			}
		},
		"mdast-util-mdx-jsx": {
			"url": "https://github.com/syntax-tree/mdast-util-mdx-jsx.git",
			"ref": "3.2.0",
			"purpose": "Oracle for @effected/markdown MDX support: the mdxJsxFlowElement/mdxJsxTextElement node contract and the reference toMarkdown serialization (attribute quoting, expression braces) the kit's MDX vocabulary and stringify must match",
			"orientation": {
				"keyPaths": {
					"serializer-and-node-model": "lib/index.js",
					"type-contract": "complex-types.d.ts",
					"fixtures": "test"
				},
				"startHere": "lib/index.js holds the node model (fromMarkdown assembly) and the toMarkdown handlers — attribute quoting, value-expression braces, printWidth handling — that the @effected/markdown MDX stringify must match; index.d.ts / complex-types.d.ts state the exact node/attribute type shapes; test/ carries serialization fixtures usable as a conformance corpus."
			}
		},
		"mdast-util-mdx-expression": {
			"url": "https://github.com/syntax-tree/mdast-util-mdx-expression.git",
			"ref": "2.0.1",
			"purpose": "Oracle for @effected/markdown MDX support: the mdxFlowExpression/mdxTextExpression node contract and reference serialization ({expression} braces) for the kit's MDX expression nodes",
			"orientation": {
				"keyPaths": {
					"serializer-and-node-model": "lib/index.js",
					"type-contract": "complex-types.d.ts",
					"fixtures": "test"
				},
				"startHere": "lib/index.js is the whole reference (node assembly + toMarkdown handlers for both expression nodes); complex-types.d.ts states the node type shapes; test/ has round-trip fixtures."
			}
		},
		"mdast-util-mdxjs-esm": {
			"url": "https://github.com/syntax-tree/mdast-util-mdxjs-esm.git",
			"ref": "2.0.1",
			"purpose": "Oracle for @effected/markdown MDX support: the mdxjsEsm node contract and reference serialization (raw ESM value passthrough) for the kit's MDX ESM node",
			"orientation": {
				"keyPaths": {
					"serializer-and-node-model": "lib/index.js",
					"type-contract": "complex-types.d.ts",
					"fixtures": "test"
				},
				"startHere": "lib/index.js is the whole reference (node assembly + toMarkdown handler for mdxjsEsm); complex-types.d.ts states the node type shape; test/ has round-trip fixtures."
			}
		},
		"spdx-license-list-data": {
			"url": "https://github.com/spdx/license-list-data.git",
			"ref": "v3.28.0",
			"purpose": "SPDX's own published license and exception metadata (json/licenses.json, json/exceptions.json) — the authority behind @effected/spdx's vendored catalog literals, carrying name, isOsiApproved, isFsfLibre and reference URLs that the spdx-license-ids devDependency (ids only) cannot supply.",
			"sparse": [
				"json/licenses.json",
				"json/exceptions.json"
			],
			"orientation": {
				"layout": "Sparse checkout: json/licenses.json and json/exceptions.json only — the two published catalog documents. Everything else in the upstream repo (per-license details/, text/, html/, rdfa/) is deliberately excluded; it is license TEXT, which this kit does not vendor.",
				"keyPaths": {
					"licenses": "json/licenses.json",
					"exceptions": "json/exceptions.json"
				},
				"startHere": "json/licenses.json is the metadata authority: each entry carries licenseId, name, isOsiApproved, isFsfLibre (present only when true), reference (the https://spdx.org/licenses/<id>.html page) and isDeprecatedLicenseId. packages/spdx/scripts/generate-data.ts reads it to regenerate the vendored catalog literal. IDs still come from the spdx-license-ids devDependency, NOT from here — that keeps the differential oracle test's id provenance unchanged; this repo supplies metadata for those ids only."
			}
		},
		"schemaorg": {
			"url": "https://github.com/schemaorg/schemaorg.git",
			"ref": "v30.0",
			"purpose": "The schema.org vocabulary itself, as published: the machine-readable JSON-LD release that defines every type, every property, and the domainIncludes/rangeIncludes relations between them — the authority @effected/schema-org's node vocabulary mirrors and its offline conformance validator checks against.",
			"sparse": [
				"data/releases/30.0/schemaorg-current-https.jsonld",
				"data/releases/30.0/schemaorg-all-https.jsonld"
			],
			"orientation": {
				"layout": "Sparse checkout: the published JSON-LD release documents under data/releases/30.0/ only. The upstream repo also carries the site generator, the .ttl sources, per-term example files and every historical release — all deliberately excluded. `schemaorg-current-https.jsonld` is the core vocabulary; `schemaorg-all-https.jsonld` additionally carries retired and pending terms.",
				"keyPaths": {
					"vocabulary": "data/releases/30.0/schemaorg-current-https.jsonld",
					"vocabulary-with-retired": "data/releases/30.0/schemaorg-all-https.jsonld"
				},
				"startHere": "data/releases/30.0/schemaorg-current-https.jsonld is the authority. It is a single JSON-LD document whose @graph holds every term: entries with @type rdfs:Class are the types, entries with @type rdf:Property are the properties, and a property's schema:domainIncludes names the types it is legal on (schema:rangeIncludes names what it may hold). That domainIncludes relation is exactly what @effected/schema-org's conformance validator checks — the real failure mode is not malformed JSON but a plausible-looking property schema.org does not define on that type. A property may carry MULTIPLE domainIncludes entries, and inherited properties come via rdfs:subClassOf, so a validator that ignores the class hierarchy will produce false rejections."
			}
		}
	}
}
