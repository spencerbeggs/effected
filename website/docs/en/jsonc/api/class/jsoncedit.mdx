---
title: "JsoncEdit | Class | API | @effected/jsonc"
description: "A non-mutating text edit: replace the span `[offset, offset + length)` with `content`. Set `length` to `0` to insert, `content` to `\"\"` to delete."
head:
  - - meta
    - property: "og:url"
      content: "https://effected.spencerbeg.gs/jsonc/api/class/jsoncedit"
  - - meta
    - property: "og:type"
      content: article
  - - meta
    - property: "og:description"
      content: "A non-mutating text edit: replace the span `[offset, offset + length)` with `content`. Set `length` to `0` to insert, `content` to `\"\"` to delete."
  - - meta
    - property: "og:image"
      content: "https://effected.spencerbeg.gs/og-image.png"
  - - meta
    - property: "og:image:alt"
      content: "@effected/jsonc - @effected/jsonc API Documentation"
  - - meta
    - property: "article:published_time"
      content: "2026-07-07T09:03:50.252Z"
  - - meta
    - property: "article:modified_time"
      content: "2026-07-07T09:03:50.252Z"
  - - meta
    - property: "article:section"
      content: Classes
  - - meta
    - property: "article:tag"
      content: TypeScript
  - - meta
    - property: "article:tag"
      content: API
  - - meta
    - property: "article:tag"
      content: "@effected/jsonc"
---


import { SourceCode } from "@rspress/core/theme";
import { ParametersTable } from "rspress-plugin-api-extractor/runtime";
import { ApiSignature, ApiMember, ApiExample } from "rspress-plugin-api-extractor/runtime";

# JsoncEdit

A non-mutating text edit: replace the span `[offset, offset + length)` with `content`. Set `length` to `0` to insert, `content` to `""` to delete.

<ApiSignature code={"class JsoncEdit extends JsoncEdit_base {\n    static applyAll(text: string, edits: ReadonlyArray<JsoncEdit>): string;\n}"} source={"class JsoncEdit extends JsoncEdit_base {\n    static applyAll(text: string, edits: ReadonlyArray<JsoncEdit>): string;\n}"} apiScope={"jsonc"} />

## Static Methods

<ApiMember code={"static applyAll(text: string, edits: ReadonlyArray<JsoncEdit>): string;"} source={"class JsoncEdit extends JsoncEdit_base {\nstatic applyAll(text: string, edits: ReadonlyArray<JsoncEdit>): string;\n}"} apiScope={"jsonc"} memberName={"applyAll"} summary={"Apply `edits` to `text`, producing a new string. Edits are applied in reverse-offset order so earlier offsets stay valid; the input `edits` array is not mutated. Pure and total."} id={"applyall"} hasParameters={false} />

