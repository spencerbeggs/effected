---
title: "Jsonc | Class | API | @effected/jsonc"
description: "Static entry points for JSONC parsing, editing-adjacent utilities and the schema factories. Not instantiable."
head:
  - - meta
    - property: "og:url"
      content: "https://effected.spencerbeg.gs/jsonc/api/class/jsonc"
  - - meta
    - property: "og:type"
      content: article
  - - meta
    - property: "og:description"
      content: "Static entry points for JSONC parsing, editing-adjacent utilities and the schema factories. Not instantiable."
  - - meta
    - property: "og:image"
      content: "https://effected.spencerbeg.gs/og-image.png"
  - - meta
    - property: "og:image:alt"
      content: "@effected/jsonc - @effected/jsonc API Documentation"
  - - meta
    - property: "article:published_time"
      content: "2026-07-07T09:03:50.252Z"
  - - meta
    - property: "article:modified_time"
      content: "2026-07-07T09:03:50.252Z"
  - - meta
    - property: "article:section"
      content: Classes
  - - meta
    - property: "article:tag"
      content: TypeScript
  - - meta
    - property: "article:tag"
      content: API
  - - meta
    - property: "article:tag"
      content: "@effected/jsonc"
---


import { SourceCode } from "@rspress/core/theme";
import { ParametersTable } from "rspress-plugin-api-extractor/runtime";
import { ApiSignature, ApiMember, ApiExample } from "rspress-plugin-api-extractor/runtime";

# Jsonc

Static entry points for JSONC parsing, editing-adjacent utilities and the schema factories. Not instantiable.

<ApiSignature code={"class Jsonc {\n    static readonly JsoncFromString: Schema.Codec<unknown, string>;\n    static readonly parse: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<unknown, JsoncParseError, never>;\n    static readonly parseTree: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<Option.None<JsoncNode> | Option.Some<JsoncNode>, JsoncParseError, never>;\n    static equals(a: string, b: string): boolean;\n    static equalsValue(text: string, value: unknown): boolean;\n    static fromString(options?: JsoncParseOptions): Schema.Codec<unknown, string>;\n    static schema<T, E>(target: Schema.Codec<T, E>, options?: JsoncParseOptions): Schema.Codec<T, string>;\n    static stripComments(text: string, replaceCh?: string): string;\n}"} source={"class Jsonc {\n    static readonly JsoncFromString: Schema.Codec<unknown, string>;\n    static readonly parse: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<unknown, JsoncParseError, never>;\n    static readonly parseTree: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<Option.None<JsoncNode> | Option.Some<JsoncNode>, JsoncParseError, never>;\n    static equals(a: string, b: string): boolean;\n    static equalsValue(text: string, value: unknown): boolean;\n    static fromString(options?: JsoncParseOptions): Schema.Codec<unknown, string>;\n    static schema<T, E>(target: Schema.Codec<T, E>, options?: JsoncParseOptions): Schema.Codec<T, string>;\n    static stripComments(text: string, replaceCh?: string): string;\n}"} apiScope={"jsonc"} />

## Static Properties

<ApiMember code={"static readonly JsoncFromString: Schema.Codec<unknown, string>;"} source={"class Jsonc {\nstatic readonly JsoncFromString: Schema.Codec<unknown, string>;\n}"} apiScope={"jsonc"} memberName={"JsoncFromString"} summary={"The zero-config `Schema<unknown, string>` — `Jsonc.fromString()` with default options, pre-bound so the common case needs no memoization discipline."} id={"jsoncfromstring"} />

<ApiMember code={"static readonly parse: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<unknown, JsoncParseError, never>;"} source={"class Jsonc {\nstatic readonly parse: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<unknown, JsoncParseError, never>;\n}"} apiScope={"jsonc"} memberName={"parse"} summary={"Parse JSONC into a plain JavaScript value. Error-recovery parsing: collects every parse error and fails once with the aggregate [JsoncParseError](/jsonc/api/class/jsoncparseerror). Returns `unknown`, never `any`."} id={"parse"} />

<ApiMember code={"static readonly parseTree: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<Option.None<JsoncNode> | Option.Some<JsoncNode>, JsoncParseError, never>;"} source={"class Jsonc {\nstatic readonly parseTree: (text: string, options?: JsoncParseOptions | undefined) => Effect.Effect<Option.None<JsoncNode> | Option.Some<JsoncNode>, JsoncParseError, never>;\n}"} apiScope={"jsonc"} memberName={"parseTree"} summary={"Parse JSONC into an immutable [JsoncNode](/jsonc/api/class/jsoncnode) AST. `Option.none()` for empty input (with `allowEmptyContent`); the aggregate [JsoncParseError](/jsonc/api/class/jsoncparseerror) for malformed input."} id={"parsetree"} />

## Static Methods

<ApiMember code={"static equals(a: string, b: string): boolean;"} source={"class Jsonc {\nstatic equals(a: string, b: string): boolean;\n}"} apiScope={"jsonc"} memberName={"equals"} summary={"Compare two JSONC strings for semantic equality: comments, whitespace, formatting and object key order are ignored; array order is significant. Best-effort — compares the recovered parse of each input. Pure and total."} id={"equals"} hasParameters={false} />

<ApiMember code={"static equalsValue(text: string, value: unknown): boolean;"} source={"class Jsonc {\nstatic equalsValue(text: string, value: unknown): boolean;\n}"} apiScope={"jsonc"} memberName={"equalsValue"} summary={"Compare a JSONC string against an existing JavaScript value with the same semantics as [Jsonc](/jsonc/api/class/jsonc). Pure and total."} id={"equalsvalue"} hasParameters={false} />

<ApiMember code={"static fromString(options?: JsoncParseOptions): Schema.Codec<unknown, string>;"} source={"class Jsonc {\nstatic fromString(options?: JsoncParseOptions): Schema.Codec<unknown, string>;\n}"} apiScope={"jsonc"} memberName={"fromString"} summary={"A `Schema<unknown, string>` decoding JSONC with the given `options` (defaults when omitted). Encoding is `JSON.stringify` with 2-space indent, so comments do not survive a round-trip encode. Schema-producing: each call returns a fresh schema whose derivation caches are not shared across calls. Bind the result to a `const` on hot paths; for the default-options case use [Jsonc](/jsonc/api/class/jsonc)."} id={"fromstring"} hasParameters={false} />

<ApiMember code={"static schema<T, E>(target: Schema.Codec<T, E>, options?: JsoncParseOptions): Schema.Codec<T, string>;"} source={"class Jsonc {\nstatic schema<T, E>(target: Schema.Codec<T, E>, options?: JsoncParseOptions): Schema.Codec<T, string>;\n}"} apiScope={"jsonc"} memberName={"schema"} summary={"Compose [Jsonc](/jsonc/api/class/jsonc) with a target schema, yielding a `Schema<A, string>` that decodes JSONC straight into a validated domain value — the reason an Effect-native JSONC library exists. Schema-producing: bind the result to a `const` on hot paths (see [Jsonc](/jsonc/api/class/jsonc))."} id={"schema"} hasParameters={false} />

<ApiMember code={"static stripComments(text: string, replaceCh?: string): string;"} source={"class Jsonc {\nstatic stripComments(text: string, replaceCh?: string): string;\n}"} apiScope={"jsonc"} memberName={"stripComments"} summary={"Remove all comments from JSONC, producing valid JSON. Pass a `replaceCh` (e.g. `\" \"`) to replace each comment character instead of deleting it, keeping all offsets stable (line breaks inside block comments are kept). Pure and total."} id={"stripcomments"} hasParameters={false} />

## Examples

<ApiExample code={"import { Jsonc } from \"@effected/jsonc\";\nimport { Effect } from \"effect\";\n\nconst program = Effect.gen(function* () {\n  const value = yield* Jsonc.parse('{ \"port\": 3000 // dev\\n }');\n\n  return value; // { port: 3000 }\n});"} source={"// @noErrors\nimport { Jsonc } from \"@effected/jsonc\";\nimport { Effect } from \"effect\";\n\nconst program = Effect.gen(function* () {\n  const value = yield* Jsonc.parse('{ \"port\": 3000 // dev\\n }');\n\n  return value; // { port: 3000 }\n});"} apiScope={"jsonc"} />

