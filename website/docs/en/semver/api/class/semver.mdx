---
title: "SemVer | Class | API | @effected/semver"
description: "A parsed SemVer 2.0.0 version: an Effect `Schema.Class` whose fields are validated in-schema (non-negative integer components, well-formed identifiers), so `SemVer.make` only produces valid versions. Instance methods are the canonical API; cross-cutting operations exist as dual statics on the class. The string representation is the schema's encoded form via SemVer."
head:
  - - meta
    - property: "og:url"
      content: "https://effected.spencerbeg.gs/semver/api/class/semver"
  - - meta
    - property: "og:type"
      content: article
  - - meta
    - property: "og:description"
      content: "A parsed SemVer 2.0.0 version: an Effect `Schema.Class` whose fields are validated in-schema (non-negative integer components, well-formed identifiers), so `SemVer.make` only produces valid versions. Instance methods are the canonical API; cross-cutting operations exist as dual statics on the class. The string representation is the schema's encoded form via SemVer."
  - - meta
    - property: "og:image"
      content: "https://effected.spencerbeg.gs/og-image.png"
  - - meta
    - property: "og:image:alt"
      content: "@effected/semver - @effected/semver API Documentation"
  - - meta
    - property: "article:published_time"
      content: "2026-07-07T07:28:51.022Z"
  - - meta
    - property: "article:modified_time"
      content: "2026-07-07T07:28:51.022Z"
  - - meta
    - property: "article:section"
      content: Classes
  - - meta
    - property: "article:tag"
      content: TypeScript
  - - meta
    - property: "article:tag"
      content: API
  - - meta
    - property: "article:tag"
      content: "@effected/semver"
---


import { SourceCode } from "@rspress/core/theme";
import { ParametersTable } from "rspress-plugin-api-extractor/runtime";
import { ApiSignature, ApiMember, ApiExample } from "rspress-plugin-api-extractor/runtime";

# SemVer

A parsed SemVer 2.0.0 version: an Effect `Schema.Class` whose fields are validated in-schema (non-negative integer components, well-formed identifiers), so `SemVer.make` only produces valid versions. Instance methods are the canonical API; cross-cutting operations exist as dual statics on the class. The string representation is the schema's encoded form via SemVer.

<ApiSignature code={"class SemVer extends SemVer_base {\n    static readonly compare: {\n    (that: SemVer): (self: SemVer) => -1 | 0 | 1;\n    (self: SemVer, that: SemVer): -1 | 0 | 1;\n  };\n    static readonly equal: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly FromString: Schema.Codec<SemVer, string>;\n    static readonly gt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly gte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly lt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly lte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly neq: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly Order: Order.Order<SemVer>;\n    static readonly OrderWithBuild: Order.Order<SemVer>;\n    static readonly parse: (input: string) => Effect.Effect<SemVer, InvalidVersionError, never>;\n    static readonly truncate: {\n    (level: \"prerelease\" | \"build\"): (self: SemVer) => SemVer;\n    (self: SemVer, level: \"prerelease\" | \"build\"): SemVer;\n  };\n    static groupBy(versions: ReadonlyArray<SemVer>, strategy: \"major\" | \"minor\" | \"patch\"): Record<string, ReadonlyArray<SemVer>>;\n    static latestByMajor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n    static latestByMinor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n    static max(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n    static min(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n    static of(major: number, minor: number, patch: number, prerelease?: ReadonlyArray<string | number>, build?: ReadonlyArray<string>): SemVer;\n    static rsort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n    static sort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n    get bump(): SemVerBump;\n    get isPrerelease(): boolean;\n    get isStable(): boolean;\n    [Equal.symbol](that: unknown): boolean;\n    compare(that: SemVer): -1 | 0 | 1;\n    equal(that: SemVer): boolean;\n    gt(that: SemVer): boolean;\n    gte(that: SemVer): boolean;\n    lt(that: SemVer): boolean;\n    lte(that: SemVer): boolean;\n    neq(that: SemVer): boolean;\n    toString(): string;\n}"} source={"class SemVer extends SemVer_base {\n    static readonly compare: {\n    (that: SemVer): (self: SemVer) => -1 | 0 | 1;\n    (self: SemVer, that: SemVer): -1 | 0 | 1;\n  };\n    static readonly equal: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly FromString: Schema.Codec<SemVer, string>;\n    static readonly gt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly gte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly lt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly lte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly neq: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n    static readonly Order: Order.Order<SemVer>;\n    static readonly OrderWithBuild: Order.Order<SemVer>;\n    static readonly parse: (input: string) => Effect.Effect<SemVer, InvalidVersionError, never>;\n    static readonly truncate: {\n    (level: \"prerelease\" | \"build\"): (self: SemVer) => SemVer;\n    (self: SemVer, level: \"prerelease\" | \"build\"): SemVer;\n  };\n    static groupBy(versions: ReadonlyArray<SemVer>, strategy: \"major\" | \"minor\" | \"patch\"): Record<string, ReadonlyArray<SemVer>>;\n    static latestByMajor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n    static latestByMinor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n    static max(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n    static min(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n    static of(major: number, minor: number, patch: number, prerelease?: ReadonlyArray<string | number>, build?: ReadonlyArray<string>): SemVer;\n    static rsort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n    static sort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n    get bump(): SemVerBump;\n    get isPrerelease(): boolean;\n    get isStable(): boolean;\n    [Equal.symbol](that: unknown): boolean;\n    compare(that: SemVer): -1 | 0 | 1;\n    equal(that: SemVer): boolean;\n    gt(that: SemVer): boolean;\n    gte(that: SemVer): boolean;\n    lt(that: SemVer): boolean;\n    lte(that: SemVer): boolean;\n    neq(that: SemVer): boolean;\n    toString(): string;\n}"} apiScope={"semver"} />

## Static Properties

<ApiMember code={"static readonly compare: {\n    (that: SemVer): (self: SemVer) => -1 | 0 | 1;\n    (self: SemVer, that: SemVer): -1 | 0 | 1;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly compare: {\n    (that: SemVer): (self: SemVer) => -1 | 0 | 1;\n    (self: SemVer, that: SemVer): -1 | 0 | 1;\n  };\n}"} apiScope={"semver"} memberName={"compare"} summary={"Compare two versions. Returns `-1`, `0`, or `1`. Dual API."} id={"static-property-compare"} />

<ApiMember code={"static readonly equal: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly equal: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"equal"} summary={"Test whether two versions are equal (ignores build metadata). Dual API."} id={"static-property-equal"} />

<ApiMember code={"static readonly FromString: Schema.Codec<SemVer, string>;"} source={"class SemVer extends SemVer_base {\nstatic readonly FromString: Schema.Codec<SemVer, string>;\n}"} apiScope={"semver"} memberName={"FromString"} summary={"Schema transformation between the canonical version string and [SemVer](/semver/api/class/semver): decoding parses with the strict grammar, encoding prints `major.minor.patch[-prerelease][+build]`."} id={"fromstring"} />

<ApiMember code={"static readonly gt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly gt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"gt"} summary={"Test whether `self > that`. Dual API."} id={"static-property-gt"} />

<ApiMember code={"static readonly gte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly gte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"gte"} summary={"Test whether `self >= that`. Dual API."} id={"static-property-gte"} />

<ApiMember code={"static readonly lt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly lt: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"lt"} summary={"Test whether `self < that`. Dual API."} id={"static-property-lt"} />

<ApiMember code={"static readonly lte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly lte: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"lte"} summary={"Test whether `self <= that`. Dual API."} id={"static-property-lte"} />

<ApiMember code={"static readonly neq: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly neq: {\n    (that: SemVer): (self: SemVer) => boolean;\n    (self: SemVer, that: SemVer): boolean;\n  };\n}"} apiScope={"semver"} memberName={"neq"} summary={"Test whether two versions are not equal (ignores build metadata). Dual API."} id={"static-property-neq"} />

<ApiMember code={"static readonly Order: Order.Order<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic readonly Order: Order.Order<SemVer>;\n}"} apiScope={"semver"} memberName={"Order"} summary={"`Order` instance following [SemVer](/semver/api/class/semver) 2.0.0 precedence (§11); build metadata is ignored (§10)."} id={"order"} />

<ApiMember code={"static readonly OrderWithBuild: Order.Order<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic readonly OrderWithBuild: Order.Order<SemVer>;\n}"} apiScope={"semver"} memberName={"OrderWithBuild"} summary={"`Order` instance that additionally compares build metadata lexically when versions are otherwise equal, producing a total order over distinct version strings. Not spec precedence — use [SemVer](/semver/api/class/semver) unless a deterministic tiebreak across build metadata is required."} id={"orderwithbuild"} />

<ApiMember code={"static readonly parse: (input: string) => Effect.Effect<SemVer, InvalidVersionError, never>;"} source={"class SemVer extends SemVer_base {\nstatic readonly parse: (input: string) => Effect.Effect<SemVer, InvalidVersionError, never>;\n}"} apiScope={"semver"} memberName={"parse"} summary={"Parse a strict [SemVer](/semver/api/class/semver) 2.0.0 version string. Rejects `v`/`V` prefixes, `=` prefixes, leading zeros on numeric identifiers and partially consumed input."} id={"parse"} />

<ApiMember code={"static readonly truncate: {\n    (level: \"prerelease\" | \"build\"): (self: SemVer) => SemVer;\n    (self: SemVer, level: \"prerelease\" | \"build\"): SemVer;\n  };"} source={"class SemVer extends SemVer_base {\nstatic readonly truncate: {\n    (level: \"prerelease\" | \"build\"): (self: SemVer) => SemVer;\n    (self: SemVer, level: \"prerelease\" | \"build\"): SemVer;\n  };\n}"} apiScope={"semver"} memberName={"truncate"} summary={"Strip components below the given level: `\"prerelease\"` keeps only `major.minor.patch`, `\"build\"` keeps the prerelease but drops build metadata. Dual API."} id={"truncate"} />

## Static Methods

<ApiMember code={"static groupBy(versions: ReadonlyArray<SemVer>, strategy: \"major\" | \"minor\" | \"patch\"): Record<string, ReadonlyArray<SemVer>>;"} source={"class SemVer extends SemVer_base {\nstatic groupBy(versions: ReadonlyArray<SemVer>, strategy: \"major\" | \"minor\" | \"patch\"): Record<string, ReadonlyArray<SemVer>>;\n}"} apiScope={"semver"} memberName={"groupBy"} summary={"Group versions by major (`\"1\"`), major.minor (`\"1.2\"`) or major.minor.patch (`\"1.2.3\"`) key. Groups and their members are in ascending precedence order. Pure derivation — an immutable record, not a service operation."} id={"groupby"} hasParameters={false} />

<ApiMember code={"static latestByMajor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic latestByMajor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n}"} apiScope={"semver"} memberName={"latestByMajor"} summary={"The highest version for each distinct major version, ascending."} id={"latestbymajor"} hasParameters={false} />

<ApiMember code={"static latestByMinor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic latestByMinor(versions: ReadonlyArray<SemVer>): ReadonlyArray<SemVer>;\n}"} apiScope={"semver"} memberName={"latestByMinor"} summary={"The highest version for each distinct major.minor pair, ascending."} id={"latestbyminor"} hasParameters={false} />

<ApiMember code={"static max(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic max(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n}"} apiScope={"semver"} memberName={"max"} summary={"Highest version, or `Option.none()` if the array is empty."} id={"max"} hasParameters={false} />

<ApiMember code={"static min(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic min(versions: ReadonlyArray<SemVer>): Option.Option<SemVer>;\n}"} apiScope={"semver"} memberName={"min"} summary={"Lowest version, or `Option.none()` if the array is empty."} id={"min"} hasParameters={false} />

<ApiMember code={"static of(major: number, minor: number, patch: number, prerelease?: ReadonlyArray<string | number>, build?: ReadonlyArray<string>): SemVer;"} source={"class SemVer extends SemVer_base {\nstatic of(major: number, minor: number, patch: number, prerelease?: ReadonlyArray<string | number>, build?: ReadonlyArray<string>): SemVer;\n}"} apiScope={"semver"} memberName={"of"} summary={"Positional convenience constructor: `SemVer.of(1, 2, 3)`."} id={"of"} hasParameters={false} />

<ApiMember code={"static rsort(versions: ReadonlyArray<SemVer>): Array<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic rsort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n}"} apiScope={"semver"} memberName={"rsort"} summary={"Sort versions descending by [SemVer](/semver/api/class/semver) precedence. Returns a new array."} id={"rsort"} hasParameters={false} />

<ApiMember code={"static sort(versions: ReadonlyArray<SemVer>): Array<SemVer>;"} source={"class SemVer extends SemVer_base {\nstatic sort(versions: ReadonlyArray<SemVer>): Array<SemVer>;\n}"} apiScope={"semver"} memberName={"sort"} summary={"Sort versions ascending by [SemVer](/semver/api/class/semver) precedence. Returns a new array."} id={"sort"} hasParameters={false} />

## Properties

<ApiMember code={"get bump(): SemVerBump;"} source={"class SemVer extends SemVer_base {\nget bump(): SemVerBump;\n}"} apiScope={"semver"} memberName={"bump"} summary={"Version bumping operations, grouped for discoverability."} id={"bump"} />

<ApiMember code={"get isPrerelease(): boolean;"} source={"class SemVer extends SemVer_base {\nget isPrerelease(): boolean;\n}"} apiScope={"semver"} memberName={"isPrerelease"} summary={"Whether this is a prerelease version."} id={"isprerelease"} />

<ApiMember code={"get isStable(): boolean;"} source={"class SemVer extends SemVer_base {\nget isStable(): boolean;\n}"} apiScope={"semver"} memberName={"isStable"} summary={"Whether this is a stable (non-prerelease) version."} id={"isstable"} />

## Methods

<ApiMember code={"[Equal.symbol](that: unknown): boolean;"} source={"class SemVer extends SemVer_base {\n[Equal.symbol](that: unknown): boolean;\n}"} apiScope={"semver"} memberName={"__computed"} id={"__computed"} hasParameters={true} />

<ParametersTable parameters={[{"name":"that","type":"unknown","description":""}]} />

<ApiMember code={"compare(that: SemVer): -1 | 0 | 1;"} source={"class SemVer extends SemVer_base {\ncompare(that: SemVer): -1 | 0 | 1;\n}"} apiScope={"semver"} memberName={"compare"} summary={"Compare `this` to `that` per [SemVer](/semver/api/class/semver) 2.0.0 precedence. Returns `-1`, `0`, or `1`."} id={"static-property-compare"} hasParameters={false} />

<ApiMember code={"equal(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\nequal(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"equal"} summary={"Test whether `this` equals `that` (ignores build metadata)."} id={"static-property-equal"} hasParameters={false} />

<ApiMember code={"gt(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\ngt(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"gt"} summary={"Test whether `this > that`."} id={"static-property-gt"} hasParameters={false} />

<ApiMember code={"gte(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\ngte(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"gte"} summary={"Test whether `this >= that`."} id={"static-property-gte"} hasParameters={false} />

<ApiMember code={"lt(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\nlt(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"lt"} summary={"Test whether `this < that`."} id={"static-property-lt"} hasParameters={false} />

<ApiMember code={"lte(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\nlte(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"lte"} summary={"Test whether `this <= that`."} id={"static-property-lte"} hasParameters={false} />

<ApiMember code={"neq(that: SemVer): boolean;"} source={"class SemVer extends SemVer_base {\nneq(that: SemVer): boolean;\n}"} apiScope={"semver"} memberName={"neq"} summary={"Test whether `this` does not equal `that` (ignores build metadata)."} id={"static-property-neq"} hasParameters={false} />

<ApiMember code={"toString(): string;"} source={"class SemVer extends SemVer_base {\ntoString(): string;\n}"} apiScope={"semver"} memberName={"toString"} summary={"The canonical `major.minor.patch[-prerelease][+build]` string."} id={"tostring"} hasParameters={false} />

## Examples

<ApiExample code={"import { SemVer } from \"@effected/semver\";\nimport { Effect } from \"effect\";\n\nconst program = Effect.gen(function* () {\n  const v = yield* SemVer.parse(\"1.2.3\");\n  const next = v.bump.minor(); // 1.3.0\n  console.log(v.gt(next)); // false\n  console.log(next.isStable); // true\n});"} source={"// @noErrors\nimport { SemVer } from \"@effected/semver\";\nimport { Effect } from \"effect\";\n\nconst program = Effect.gen(function* () {\n  const v = yield* SemVer.parse(\"1.2.3\");\n  const next = v.bump.minor(); // 1.3.0\n  console.log(v.gt(next)); // false\n  console.log(next.isStable); // true\n});"} apiScope={"semver"} />

