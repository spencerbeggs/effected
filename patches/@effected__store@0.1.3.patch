diff --git a/Cache.js b/Cache.js
index 5b05f5b..0829b23 100644
--- a/Cache.js
+++ b/Cache.js
@@ -86,7 +86,7 @@ var CacheEvent = class extends Schema.Class("CacheEvent")({
 *
 * @public
 */
-var CacheError = class extends Schema.TaggedErrorClass()("CacheError", {
+var CacheError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("CacheError", {
 	/** The cache operation that failed. */
 	operation: Schema.Literals([
 		"setup",
diff --git a/Store.js b/Store.js
index ec93025..e3e3d61 100644
--- a/Store.js
+++ b/Store.js
@@ -28,7 +28,7 @@ var StoreMigrationStatus = class extends Schema.Class("StoreMigrationStatus")({
 *
 * @public
 */
-var StoreError = class extends Schema.TaggedErrorClass()("StoreError", {
+var StoreError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("StoreError", {
 	/** The store operation that failed. */
 	operation: Schema.Literals([
 		"setup",
@@ -55,7 +55,7 @@ var StoreError = class extends Schema.TaggedErrorClass()("StoreError", {
 *
 * @public
 */
-var StoreMigrationError = class extends Schema.TaggedErrorClass()("StoreMigrationError", {
+var StoreMigrationError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("StoreMigrationError", {
 	/** Whether the failure happened applying (`up`) or unwinding (`down`). */
 	direction: Schema.Literals(["up", "down"]),
 	/** The failing migration's id. */
