diff --git a/ManagedSection.js b/ManagedSection.js
index 84087bd..a112874 100644
--- a/ManagedSection.js
+++ b/ManagedSection.js
@@ -18,7 +18,7 @@ import { Context, Effect, FileSystem, Layer, Schema } from "effect";
 *
 * @public
 */
-var SectionFileError = class extends Schema.TaggedErrorClass()("SectionFileError", {
+var SectionFileError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("SectionFileError", {
 	/** The file the operation was against. */
 	path: Schema.String,
 	/** Which half of the read-modify-write failed. */
diff --git a/SectionDialect.js b/SectionDialect.js
index ea93b8a..9f8b2f1 100644
--- a/SectionDialect.js
+++ b/SectionDialect.js
@@ -12,7 +12,7 @@ import { Equal, Result, Schema } from "effect";
 *
 * @public
 */
-var SectionRenderError = class extends Schema.TaggedErrorClass()("SectionRenderError", {
+var SectionRenderError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("SectionRenderError", {
 	/**
 	* `markerInContent` — the content carries a line the scanner would read as
 	* a marker, which would move the block boundary and let the next sync
diff --git a/SectionDocument.js b/SectionDocument.js
index 7afc984..0346cdf 100644
--- a/SectionDocument.js
+++ b/SectionDocument.js
@@ -20,7 +20,7 @@ import { Effect, Equal, Option, Result, Schema } from "effect";
 *
 * @public
 */
-var SectionParseError = class SectionParseError extends Schema.TaggedErrorClass()("SectionParseError", {
+var SectionParseError = class SectionParseError extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("SectionParseError", {
 	/** Which ambiguity was found. */
 	reason: Schema.Literals(SCAN_FAILURE_REASONS),
 	/** 1-based line of the offending marker. */
