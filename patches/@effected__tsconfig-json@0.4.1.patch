diff --git a/TsconfigJson.js b/TsconfigJson.js
index f25da80..0dc8fd0 100644
--- a/TsconfigJson.js
+++ b/TsconfigJson.js
@@ -107,7 +107,7 @@ const TsconfigJsonFromString = Jsonc.schema(TsconfigJson);
 *
 * @public
 */
-var TsconfigParseError = class extends Schema.TaggedErrorClass()("TsconfigParseError", {
+var TsconfigParseError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("TsconfigParseError", {
 	/** The file path that failed to parse, or `""` when not file-bound. */
 	path: Schema.String,
 	/** The underlying decode failure. */
diff --git a/TsconfigLoader.js b/TsconfigLoader.js
index fe6265d..2283759 100644
--- a/TsconfigLoader.js
+++ b/TsconfigLoader.js
@@ -21,7 +21,7 @@ const MAX_EXTENDS_DEPTH = 32;
 *
 * @public
 */
-var TsconfigExtendsError = class extends Schema.TaggedErrorClass()("TsconfigExtendsError", {
+var TsconfigExtendsError = class extends (Schema.TaggedErrorClass ?? Schema.TaggedError)()("TsconfigExtendsError", {
 	/** The config whose `extends` could not be resolved. */
 	path: Schema.String,
 	/** The target that failed: the spec, the re-entered path, or the refused path. */
